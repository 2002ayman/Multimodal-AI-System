# Multimodal-AI-System
Upload single or multiple images at once.  Automatically generate captions, extract features, and detect text for each image.  Save all results into a local SQLite database.  Search stored images using captions, OCR text, or semantic similarity with CLIP.  Browse and view all previously stored images.
